global:
  resolve_timeout: 5m

route:
  receiver: "slack-alerts"

  routes:
    - receiver: "slack-alerts"
      match:
        severity: "Critical"

receivers:
  - name: "slack-alerts"
    slack_configs:
      - api_url: "${SLACK_WEBHOOK_URL}"
        send_resolved: true
